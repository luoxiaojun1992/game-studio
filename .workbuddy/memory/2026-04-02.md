# 2026-04-02 工作日志

## 修复 6 个 Bug + 新功能

1. **Bug1 指令页面历史记录**: 重写 CommandPanel，切换Agent时加载历史消息，发送新消息保留历史而非清空
2. **Bug2 策划案/游戏成品保存**: 创建 output/ 产出目录，提案和游戏提交/审批时自动保存到文件，通过 /output 静态服务访问
3. **Bug3 交接确认功能**: 拆分 accept+confirm 两步，前端显示确认提示框，新增 confirm API 端点
4. **Bug4 清除聊天记录**: 新增 DELETE /api/agents/:id/messages API，CommandPanel 增加清除按钮
5. **Bug5 Agent长期记忆**: 新增 agent_memories 表和 CRUD API，系统提示词追加记忆保存指引，canUseTool 自动放行记忆API
6. **Bug6 产出目录**: output/ 加入 .gitignore，express.static 提供访问

## 架构重构: SDK Custom Tools 替代 curl hack

**问题**: 之前 Agent 通过 Bash+curl 调用 localhost:3000 API 实现交接、记忆等功能（"两套 skill 调用"）
**方案**: 使用 CodeBuddy Agent SDK 原生 Custom Tools（createSdkMcpServer + tool），新增 `server/tools.ts`
**改动**:
1. 新增 `server/tools.ts` — 注册 7 个 MCP 自定义工具：save_memory, get_memories, create_handoff, submit_proposal, submit_game, get_proposals, get_pending_handoffs
2. 重构 `server/agent-manager.ts` — query 时通过 mcpServers 注册工具 + systemPrompt 注入记忆 + canUseTool 简化为只放行 mcp__studio-tools__* + buildSystemPrompt 方法
3. 重构 `server/agents.ts` — 删除 50 行 curl 代码，替换为自然语言工具说明 + TOOLS_OVERVIEW 表格
4. 新增 zod 依赖（SDK Custom Tools schema 必须用 zod）
5. 前端和数据库结构零改动
