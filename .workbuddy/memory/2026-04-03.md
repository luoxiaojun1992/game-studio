# 2026-04-03 工作日志

## game-dev-studio 更新记录

### git pull 更新（09:45）
- 从 origin/main 拉取更新（3799909..cbf1e43）
- 新增 `src/components/TaskBoardPanel.tsx` 任务看板面板
- 新增 `src/config.ts` 配置文件
- `server/db.ts` 大幅更新，新增 `project_id` 列
- 旧数据库 schema 不兼容，删除 `data/studio.db` 重建
- 服务重启成功，健康检查通过

### 之前修改均已保留（Fast-forward 合并无冲突）
- `server/agent-manager.ts`：CAN_AUTO_ALLOW 自定义工具权限控制
- `src/pages/StudioPage.tsx`：connectedRef 修复 SSE 重复连接
- `src/components/CommandPanel.tsx`：workingAgent 默认选中正在工作的 Agent

### git pull 更新（13:27）
- 拉取 d3f0f18..cd4a8ba，11 个文件大幅更新（+626/-279 行）
- 新增 projects 表，多张表新增 project_id 列
- 发现并修复 db.ts bug：`MAX_PROJECT_ID_LENGTH` 常量在使用前未初始化（提前到文件顶部声明）
- 旧数据库重建，服务启动成功
- 为项目撰写了小红书推广文案

### 修复 4 个 Bug（20:44）
1. **交接请求自动放行**：`auto_handoff_enabled` 默认从 0 改为 1，交接无需人工确认
2. **工程师同步更新开发和测试任务**：systemPrompt 增加规则 7，强制双任务状态同步
3. **查询类操作自动放行**：已确认 `get_*` 工具都在 CAN_AUTO_ALLOW 列表中
4. **运行日志滚动方向修复**：LogPanel 日志列表 reverse，最新日志在顶部，自动滚动到 scrollTop=0

### 日志表合并后的代码清理（23:27）
- `agent_logs` + `stream_logs` 合并为统一的 `logs` 表后的残留代码清理
- `addStreamLogEntry` → `addAgentLogEntry`（方法名更准确，不再是"stream"专用）
- 修正 5 处引用旧表名 `stream_logs` 的注释
- 更新 `DEVELOPMENT.md` 中旧表名 `agent_logs` → `logs`
- lint 检查 0 错误
